import 'package:flutter/material.dart';

class MyEstateScreen extends StatelessWidget {
  const MyEstateScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
